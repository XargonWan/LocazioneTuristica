
services:
  locazioneturistica:
    # build: .
    image: xargonwan/locazioneturistica:latest
    container_name: locazionetursitica
    ports:
      - "8002:8000"
    volumes:
      - ./data:/data
    environment:
      - DATABASE_URL=sqlite:////data/db.sqlite3
      - SECRET_KEY=changeme
    restart: unless-stopped
